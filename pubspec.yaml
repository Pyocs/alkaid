name: alkaid
description: A simple model rendering framework.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.0.0

# Add regular dependencies here.
dependencies:
  crypto: ^3.0.3
  file: ^6.1.4
  io: ^1.0.4
  logging: ^1.1.1
  mime: ^1.0.4
  path: ^1.8.3

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
